<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yuqin.meinian.api.db.mapper.PromotionRuleMapper">

    <resultMap id="BaseResultMap" type="com.yuqin.meinian.api.db.entity.PromotionRuleEntity">
            <id property="ruleId" column="rule_id" />
            <result property="ruleName" column="rule_name" />
            <result property="ruleContent" column="rule_content" />
            <result property="remark" column="remark" />
    </resultMap>

    <sql id="Base_Column_List">
        rule_id,rule_name,rule_content,remark
    </sql>
    <select id="selectPageByQueryCondition"
            resultType="com.yuqin.meinian.api.mis.VO.PromotionRuleStatisticsVO">
        select
        r.rule_id as ruleId,count(p.id) as count,r.rule_name as ruleName,r.remark
        from
        promotion_rule r
        left join
        med_exam_package p
        on
        r.rule_id = p.promotion_id
        <where>
            <if test="ruleName != null and ruleName != ''">
                and r.rule_name like concat('%', #{ruleName}, '%')
            </if>
        </where>
        group by
        r.rule_id
        order by
        r.rule_id
    </select>
</mapper>
